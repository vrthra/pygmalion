

def learn_grammar(grammar):
    return [grammar]
    # see if we can find any rules where same
    # elt is used in one but repeated in another.
    for key in grammar:
        rules = grammar[key]


